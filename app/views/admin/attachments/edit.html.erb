<div class="mb-6">
  <%= link_to "&larr; Back to Attachment".html_safe, admin_project_attachment_path(@project, @attachment), class: "text-sm text-gray-500 hover:text-gray-700" %>
</div>

<h1 class="text-2xl font-bold text-gray-900 mb-6">Edit Attachment</h1>

<%= render "form", attachment: @attachment, project: @project %>
