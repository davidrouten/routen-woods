<div class="flex justify-between items-center mb-6">
  <h1 class="text-2xl font-bold text-gray-900"><%= t("admin.nav.testimonials") %></h1>
  <% if current_user_can?(:manage, :testimonials) %>
    <%= link_to "Add Testimonial", new_admin_testimonial_path, class: "px-4 py-2 bg-[#d4a338] text-[#0f1b2d] font-semibold rounded-lg hover:bg-[#b8860b] transition text-sm" %>
  <% end %>
</div>

<div class="bg-white rounded-xl shadow-sm overflow-hidden">
  <table class="min-w-full divide-y divide-gray-200">
    <thead class="bg-gray-50">
      <tr>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Author</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Rating</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Featured</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Position</th>
        <th class="px-6 py-3"></th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-200">
      <% @testimonials.each do |t| %>
        <tr class="hover:bg-gray-50">
          <td class="px-6 py-4">
            <p class="font-medium text-gray-900"><%= t.author_name %></p>
            <p class="text-sm text-gray-500 truncate max-w-xs"><%= truncate(t.body, length: 80) %></p>
          </td>
          <td class="px-6 py-4 text-[#d4a338]"><%= "★" * t.rating %></td>
          <td class="px-6 py-4"><%= t.featured? ? "✓" : "—" %></td>
          <td class="px-6 py-4 text-gray-500"><%= t.position %></td>
          <td class="px-6 py-4 text-right space-x-2">
            <% if current_user_can?(:manage, :testimonials) %>
              <%= link_to "Edit", edit_admin_testimonial_path(t), class: "text-[#d4a338] hover:text-[#b8860b] text-sm" %>
              <%= button_to "Delete", admin_testimonial_path(t), method: :delete, class: "text-red-500 hover:text-red-700 text-sm", data: { turbo_confirm: "Delete this testimonial?" } %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
